<%- include("../../views/partials/admin/header") %>
<style>
    .content-title {
        font-size: 28px; /* Slightly larger font size */
        font-weight: bold; /* Makes the text bold */
        color: #333; /* Neutral dark color */
        text-transform: uppercase; /* Capitalizes the text */
        text-align: center; /* Centers the text */
        margin: 20px 0; /* Adds vertical spacing */
        letter-spacing: 2px; /* Adds spacing between letters */
        border-bottom: 2px solid #28a745; /* Adds a green underline */
        display: inline-block; /* Ensures underline adjusts to text width */
        padding-bottom: 5px; /* Adds spacing between text and underline */
    }
</style>

<section class="content-main">
    <div class="content-header">
        <div>
            <h2 class="content-title card-title">DASHBOARD </h2>
        </div>
       
    </div>
</section>
<%- include("../../views/partials/admin/footer") %>